# ── Core Framework ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
aiofiles>=24.1.0

# ── Configuration ───────────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── PostgreSQL (Relational Memory) ──────────────────────────────
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.2

# ── Qdrant (Vector Memory / RAG) ───────────────────────────────
qdrant-client>=1.11.0
fastembed>=0.3.6

# ── Groq LLM + STT ─────────────────────────────────────────────
groq>=0.11.0

# ── Local STT Fallback (Optional) ──────────────────────────────
# Uncomment below if you want local Whisper inference instead of Groq STT
# faster-whisper>=1.0.3
